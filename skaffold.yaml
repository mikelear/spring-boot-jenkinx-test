apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/mikelear/spring-boot-jenkinx-test:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/mikelear/spring-boot-jenkinx-test:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-boot-jenkinx-test
        chartPath: charts/spring-boot-jenkinx-test
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/mikelear/spring-boot-jenkinx-test'
          image.tag: '{{.DIGEST_HEX}}'
